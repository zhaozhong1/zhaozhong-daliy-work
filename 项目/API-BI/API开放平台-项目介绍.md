# API开放平台-项目介绍

本项目目的是提供在线调用和SDK调用API的能力，并在后台对每个用户对每个接口的调用进行统计。同时，支持对API的快速接入。

这个项目有两个侧重点，一个是API接入方面，一个是校验鉴权方面。

在校验和鉴权方面，所有用户在调用接口时都需要申请ak/sk，在线调用和sdk调用都依赖于ak/sk对用户进行确认。同时，为了保证不受重放攻击，在请求头和签名中加入随机数和时间戳。

在API接入方面，开发者开发好API后，仅需在后台添加接口必填信息提交，即可将接口预上线。管理员在后台对接口进行更新后，网关将读到该接口信息并接入路由上线。