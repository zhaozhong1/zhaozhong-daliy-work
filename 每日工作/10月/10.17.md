# 10.17

## 1. 八股文

### 1. JUC

#### Completable 高级特性和用法 *

1. 基本用法 completed supplyAsync ...
2. 一元依赖 thenApply、thenRun...
3. 二元依赖 thenCombine
4. 多元依赖 allOf、anyOf 、->thenApply

#### AQS - CountDownLatch 、Reentrantlock *



#### Synchronized

#### 锁的状态

1. 无锁状态：该共享资源不涉及多线程访问
2. 偏向锁：共享资源首次被访问时，该共享资源设置为偏向锁，并在对象头中记录线程号。后续该线程再次访问时，会根据偏向锁标识和线程id比较是否相同，比对成功直接获取锁，进入临界区。这也是Synchronized的可重入实现。
3. 轻量级锁：当多个线程同时申请共享资源锁的访问时，这就产生了竞争，JVM会先尝试使用轻量级锁，以CAS方式来获取锁。成功则获取到锁，状态为轻量级锁，失败（达到一定的自旋次数还未成功）则锁升级到重量级锁。
4. 轻量级锁自选一定次数失败后，为减少自旋带来的性能损耗，锁会被升级为重量级锁。重量级锁由操作系统来实现，所以性能消耗相对较高。（涉及到内核/用户态上下文切换）

#### 锁升级

1. 当JVM启动后，一个共享资源对象直到有线程第一个访问时，这段时间内是处于无锁状态，对象头的Markword里偏向锁标识位是0，锁标识位是01。
2. 当一个共享资源首次被某个线程访问时，锁就会从无锁状态升级到偏向锁状态。
3. 当第二个线程尝试获取偏向锁失败时，偏向锁会升级为轻量级锁，此时，JVM会使用CAS自旋操作来尝试获取锁，如果成功则进入临界区域，否则升级为重量级锁。
4. 当轻量级锁获取锁失败时，说明有竞争存在，轻量级锁会升级为重量级锁，此时，JVM会将线程阻塞，直到获取到锁后才能进入临界区域，底层是通过操作系统的mutex lock来实现的。每个对象指向一个monitor对象，这个monitor对象在堆中与锁是关联的，通过monitorenter指令插入到同步代码块在编译后的开始位置，monitorexit指令插入到同步代码块的结束处和异常处，这两个指令配对出现。JVM的线程和操作系统的线程是对应的，重量级锁的Markword里存储的指针是这个monitor对象的地址，操作系统来控制内核态中的线程的阻塞和恢复，从而达到JVM线程的阻塞和恢复，涉及内核态和用户态的切换，影响性能，所以叫重量级锁。

##### 



#### CAS 、Atomic √

## 2. 投递 √



## 3. 每日一题

1. leetcode 75 双指针板块 
2. 150 二分板块 √
3. 快排+堆排+归并

