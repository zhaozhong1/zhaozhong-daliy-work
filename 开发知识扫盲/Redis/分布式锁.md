## 分布式锁

### 分布式锁有什么挑战

1. 服务挂掉，锁永不释放 -> 添加TTL -> 若TTL结束服务未结束 会出现问题 -> 添加看门狗机制 -> 当服务存活时，任务未执行完，则每10秒续一次期。 当服务挂掉时，则不续期。 

### 用什么实现分布式锁 

setnx、hash。这种基于k-v的，类似CAS的操作都可实现分布式锁。

### 什么特性能够实现分布式锁 

保证原子性 + 一致性 -> set/setnx 就是原子性操作，同时Redis在内存工作，能够保证一致性，因此可以做分布式锁。